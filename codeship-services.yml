stage-one:
  build:
    image: localhost:5000/stage-one
    dockerfile: Dockerfile.stage-one
  cached: true

stage-two:
  build:
    image: localhost:5000/stage-two
    dockerfile: Dockerfile.stage-two
  cached: true